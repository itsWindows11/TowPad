<ContentDialog
    x:Class="Rich_Text_Editor.SettingsDlg"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Rich_Text_Editor"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    CornerRadius="7">

    <StackPanel Orientation="Vertical">
        <TextBlock Style="{StaticResource SubheaderTextBlockStyle}" FontSize="20" FontWeight="SemiBold" Text="Settings" Margin="0,0,0,7" />
        <RelativePanel Height="300" Margin="0,0,0,7">
            <Grid x:Name="one" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="80" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Text wrapping" Margin="0,8,0,0"  />
                <ToggleSwitch x:Name="textWrappingSwitch" Grid.Column="2" IsOn="True" Toggled="ToggleSwitchTextWrap_Toggled" />
            </Grid>
            <Grid x:Name="two" RelativePanel.Below="one" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="80" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Default font" Margin="0,8,0,0"  />
                <ComboBox IsTextSearchEnabled="True" Grid.Column="2" x:Name="FontsCombo" SelectionChanged="FontsCombo_SelectionChanged" ItemsSource="{x:Bind Fonts}" PlaceholderText="Font">
                    <ComboBox.ItemTemplate>
                        <DataTemplate x:DataType="x:String">
                            <TextBlock FontFamily="{x:Bind}"
                    Text="{x:Bind}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </Grid>
        </RelativePanel>
        <TextBlock Text="NOTE: You may need to restart the app for some settings to fully take effect" />
        <Button Content="OK" x:Name="okBtn11" Margin="0,10,0,0" Click="okBtn11_Click" HorizontalAlignment="Stretch" />
    </StackPanel>
</ContentDialog>
